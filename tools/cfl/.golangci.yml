version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - revive
    - gosec
    - errorlint
    - exhaustive
  settings:
    revive:
      # Explicit rule list so behavior is stable across golangci-lint versions
      # (see tools/jtk/.golangci.yml for context).
      rules:
        - name: unused-parameter
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/open-cli-collective/confluence-cli

run:
  timeout: 5m
